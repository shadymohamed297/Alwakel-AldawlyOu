# نشر الباك إند مجانًا (بدون سيرفر شخصي)

خطوتين بس: قاعدة بيانات على **Neon** (مجانية للأبد، من غير بطاقة ائتمان)، وسيرفر الـ API
على **Render** (مجاني، من غير بطاقة ائتمان، من غير Dockerfile — بيكتشف مشروع Node تلقائي).

> ليه Neon مش قاعدة بيانات Render المجانية؟ لأن قاعدة بيانات Render المجانية **بتتمسح بعد ٣٠
> يوم** لو مترقّتش لخطة مدفوعة. Neon مجانية بشكل دائم (بدون انتهاء صلاحية) وده اللي محتاجينه
> لنظام حقيقي بيستخدمه مركز الصيانة يوميًا.

## ١. قاعدة البيانات (Neon)

1. روح على [neon.com](https://neon.com) واعمل حساب مجاني (فيه تسجيل بجوجل/جيت هاب، من غير بطاقة).
2. اعمل **New Project** — اختار أي اسم واقرب منطقة جغرافية لمصر.
3. من صفحة الـ Dashboard بتاعة المشروع، انسخ الـ **Connection string** (شكلها كده:
   `postgres://user:password@ep-xxxx.neon.tech/dbname?sslmode=require`). هتحتاجه في الخطوة الجاية.

## ٢. السيرفر (Render)

1. اربط الريبو ده بحساب GitHub بتاعك (لو لسه مربطتوش).
2. روح على [render.com](https://render.com) واعمل حساب مجاني، واربطه بحساب الـ GitHub بتاعك.
3. من الداشبورد: **New → Blueprint**، اختار الريبو ده. Render هيلاقي ملف `render.yaml`
   في جذر الريبو تلقائيًا ويجهّز خدمة اسمها `markaz-sayana-api`.
4. قبل ما تعمل Deploy، هيطلب منك تدخل قيمة `DATABASE_URL` — الصقه هنا الـ connection string
   اللي نسخته من Neon في الخطوة اللي فاتت. (`JWT_SECRET` هيتولّد تلقائي، متحتاجش تعمل حاجة فيه).
5. اضغط **Apply/Deploy**. أول مرة هتاخد ٢-٣ دقايق (بتنزّل الحزم وتشغّل `npm run migrate`
   اللي بيجهّز جداول قاعدة البيانات).
6. لما يخلص، هتلاقي رابط زي: `https://markaz-sayana-api.onrender.com` — ده عنوان الـ API بتاعك.
   جرّبه: `https://markaz-sayana-api.onrender.com/health` المفروض يرجّع `{"ok":true}`.

### تنبيه: الخدمة المجانية بتنام

الخدمة المجانية على Render بتـ"نام" لو محدش استخدمها لمدة ١٥ دقيقة، وأول طلب بعد كده بياخد
حوالي دقيقة لحد ما "تصحى". يعني أول مرة حد يفتح التطبيق بعد فترة راحة، هيستنى شوية. ده طبيعي
ومتوقع في الخطة المجانية — مش عطل.

## ٣. تعبئة البيانات لأول مرة (مرة واحدة بس)

بعد أول Deploy ناجح، لازم تشغّل الـ seed مرة واحدة عشان تتعمل حسابات الدخول والبيانات التجريبية:

1. من صفحة الخدمة على Render، روح تبويب **Shell** (بيديك طرفية شغالة جوه السيرفر نفسه).
2. اكتب: `npm run seed` واضغط Enter.
3. هيطبعلك أسماء حسابات الدخول (كلمة السر لكل الحسابات التجريبية: `Passw0rd!`).

⚠️ **متشغّلش `npm run seed` تاني بعد كده** إلا لو عايز تمسح كل البيانات الحقيقية وترجّع البيانات
التجريبية — الأمر ده بيمسح كل حاجة قبل ما يحط البيانات التجريبية.

## ٤. ربط تطبيق الأندرويد بالسيرفر

التطبيق فيه إعداد "عنوان السيرفر" في شاشة الدخول — اكتب فيه رابط Render بتاعك
(`https://markaz-sayana-api.onrender.com`) مرة واحدة بعد أول تسجيل دخول، وهيفضل محفوظ على
الجهاز. مش محتاج تعمل rebuild للتطبيق كل ما تغيّر السيرفر.

## بعد كده

- غيّر كلمة سر كل حساب حقيقي (مش الحسابات التجريبية) قبل ما تدي التطبيق للفنيين/الاستقبال.
- لو حبيت تشيل الحسابات/البيانات التجريبية وتبدأ ببيانات حقيقية فاضية، شغّل استعلام SQL
  يدوي من تبويب Shell بيمسح الصفوف التجريبية، أو ابعتلي وأظبطلك سكريبت تنظيف مخصوص.
